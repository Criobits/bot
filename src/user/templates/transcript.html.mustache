<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>#{{ channelName }} - Ticket Transcript</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'gg sans', 'Noto Sans', 'Helvetica Neue', Helvetica, Arial, sans-serif;
            background-color: #313338;
            color: #dbdee1;
            font-size: 16px;
            line-height: 1.375;
        }

        .transcript-container {
            max-width: 100%;
            min-height: 100vh;
        }

        /* Header */
        .header {
            background-color: #2b2d31;
            padding: 16px 20px;
            border-bottom: 1px solid #1e1f22;
            position: sticky;
            top: 0;
            z-index: 100;
        }

        .header-content {
            max-width: 1200px;
            margin: 0 auto;
        }

        .channel-name {
            color: #f2f3f5;
            font-size: 16px;
            font-weight: 600;
            display: flex;
            align-items: center;
            gap: 6px;
        }

        .channel-name::before {
            content: '#';
            color: #80848e;
        }

        .ticket-info {
            background-color: #2b2d31;
            padding: 16px 20px;
            border-bottom: 1px solid #1e1f22;
        }

        .ticket-info-content {
            max-width: 1200px;
            margin: 0 auto;
        }

        .ticket-info-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
            gap: 12px;
        }

        .info-item {
            display: flex;
            flex-direction: column;
            gap: 2px;
        }

        .info-label {
            color: #80848e;
            font-size: 12px;
            font-weight: 600;
            text-transform: uppercase;
        }

        .info-value {
            color: #dbdee1;
            font-size: 14px;
        }

        /* Messages container */
        .messages-container {
            max-width: 1200px;
            margin: 0 auto;
            padding: 16px 20px;
        }

        /* Message group */
        .message-group {
            display: flex;
            padding: 2px 0;
            margin-top: 17px;
            position: relative;
        }

        .message-group:first-child {
            margin-top: 0;
        }

        .message-group:hover {
            background-color: #2e3035;
        }

        /* Avatar */
        .avatar {
            width: 40px;
            height: 40px;
            border-radius: 50%;
            margin-right: 16px;
            flex-shrink: 0;
            background-color: #5865f2;
            display: flex;
            align-items: center;
            justify-content: center;
            font-weight: 600;
            color: white;
            font-size: 16px;
        }

        .avatar img {
            width: 100%;
            height: 100%;
            border-radius: 50%;
            object-fit: cover;
        }

        /* Message content */
        .message-content-wrapper {
            flex: 1;
            min-width: 0;
        }

        .message-header {
            display: flex;
            align-items: baseline;
            gap: 8px;
            margin-bottom: 2px;
        }

        .author-name {
            font-weight: 500;
            font-size: 16px;
            cursor: pointer;
        }

        .author-name:hover {
            text-decoration: underline;
        }

        .bot-tag {
            background-color: #5865f2;
            color: white;
            font-size: 10px;
            font-weight: 500;
            padding: 1px 4px;
            border-radius: 3px;
            text-transform: uppercase;
            margin-left: 4px;
        }

        .timestamp {
            color: #80848e;
            font-size: 12px;
            font-weight: 400;
        }

        .message-text {
            color: #dbdee1;
            word-wrap: break-word;
            white-space: pre-wrap;
        }

        /* Attachments */
        .attachments {
            margin-top: 8px;
            display: flex;
            flex-direction: column;
            gap: 4px;
        }

        .attachment {
            display: inline-flex;
            align-items: center;
            gap: 8px;
            background-color: #2b2d31;
            border: 1px solid #1e1f22;
            border-radius: 8px;
            padding: 10px;
            max-width: 520px;
        }

        .attachment-icon {
            width: 24px;
            height: 24px;
            color: #b5bac1;
        }

        .attachment-info {
            display: flex;
            flex-direction: column;
            min-width: 0;
        }

        .attachment-name {
            color: #00a8fc;
            font-size: 16px;
            text-decoration: none;
            overflow: hidden;
            text-overflow: ellipsis;
            white-space: nowrap;
        }

        .attachment-name:hover {
            text-decoration: underline;
        }

        .attachment-size {
            color: #80848e;
            font-size: 12px;
        }

        /* Embeds */
        .embed {
            margin-top: 8px;
            max-width: 520px;
            border-radius: 4px;
            overflow: hidden;
            border-left: 4px solid #5865f2;
            background-color: #2b2d31;
        }

        .embed-content {
            padding: 8px 16px 16px 12px;
        }

        .embed-author {
            display: flex;
            align-items: center;
            gap: 8px;
            margin-bottom: 4px;
        }

        .embed-author-icon {
            width: 24px;
            height: 24px;
            border-radius: 50%;
        }

        .embed-author-name {
            font-size: 14px;
            font-weight: 600;
            color: #f2f3f5;
        }

        .embed-title {
            font-size: 16px;
            font-weight: 600;
            color: #00a8fc;
            margin-bottom: 8px;
        }

        .embed-description {
            font-size: 14px;
            color: #dbdee1;
            line-height: 1.3;
        }

        .embed-fields {
            display: flex;
            flex-wrap: wrap;
            gap: 8px;
            margin-top: 8px;
        }

        .embed-field {
            flex-basis: 100%;
        }

        .embed-field.inline {
            flex-basis: calc(33.33% - 8px);
        }

        .embed-field-name {
            font-size: 14px;
            font-weight: 600;
            color: #f2f3f5;
            margin-bottom: 2px;
        }

        .embed-field-value {
            font-size: 14px;
            color: #dbdee1;
        }

        .embed-footer {
            margin-top: 8px;
            display: flex;
            align-items: center;
            gap: 8px;
        }

        .embed-footer-icon {
            width: 20px;
            height: 20px;
            border-radius: 50%;
        }

        .embed-footer-text {
            font-size: 12px;
            color: #b5bac1;
        }

        .embed-thumbnail {
            float: right;
            max-width: 80px;
            max-height: 80px;
            border-radius: 4px;
            margin-left: 16px;
        }

        .embed-image {
            max-width: 100%;
            max-height: 300px;
            border-radius: 4px;
            margin-top: 16px;
        }

        /* Image attachments */
        .image-attachment {
            max-width: 400px;
            max-height: 300px;
            border-radius: 8px;
            margin-top: 8px;
            cursor: pointer;
        }

        /* Questions section */
        .questions-section {
            background-color: #2b2d31;
            margin: 16px 20px;
            border-radius: 8px;
            padding: 16px;
            max-width: 1160px;
            margin-left: auto;
            margin-right: auto;
        }

        .questions-title {
            color: #f2f3f5;
            font-size: 18px;
            font-weight: 600;
            margin-bottom: 16px;
            display: flex;
            align-items: center;
            gap: 8px;
        }

        .question-item {
            margin-bottom: 12px;
            padding: 12px;
            background-color: #313338;
            border-radius: 4px;
        }

        .question-label {
            color: #80848e;
            font-size: 12px;
            font-weight: 600;
            text-transform: uppercase;
            margin-bottom: 4px;
        }

        .question-answer {
            color: #dbdee1;
            font-size: 14px;
        }

        /* Feedback section */
        .feedback-section {
            background-color: #2b2d31;
            margin: 16px 20px;
            border-radius: 8px;
            padding: 16px;
            max-width: 1160px;
            margin-left: auto;
            margin-right: auto;
        }

        .feedback-title {
            color: #f2f3f5;
            font-size: 18px;
            font-weight: 600;
            margin-bottom: 12px;
        }

        .rating-stars {
            color: #faa61a;
            font-size: 24px;
            margin-bottom: 8px;
        }

        .feedback-comment {
            color: #dbdee1;
            font-size: 14px;
            font-style: italic;
        }

        /* Message number badge */
        .message-number {
            color: #5865f2;
            font-size: 10px;
            font-weight: 600;
            background-color: #5865f233;
            padding: 2px 6px;
            border-radius: 4px;
            margin-left: 8px;
        }

        /* Pinned indicator */
        .pinned-badge {
            background-color: #faa61a33;
            color: #faa61a;
            font-size: 10px;
            font-weight: 600;
            padding: 2px 6px;
            border-radius: 4px;
            margin-left: 4px;
        }

        /* Divider */
        .divider {
            display: flex;
            align-items: center;
            margin: 16px 0;
            color: #80848e;
            font-size: 12px;
            font-weight: 600;
        }

        .divider::before,
        .divider::after {
            content: '';
            flex: 1;
            height: 1px;
            background-color: #3f4147;
        }

        .divider::before {
            margin-right: 8px;
        }

        .divider::after {
            margin-left: 8px;
        }

        /* Footer */
        .transcript-footer {
            background-color: #2b2d31;
            padding: 16px 20px;
            border-top: 1px solid #1e1f22;
            text-align: center;
            color: #80848e;
            font-size: 12px;
            margin-top: 32px;
        }

        /* Role colors for usernames */
        {{#ticket.archivedRoles}}
        .role-{{roleId}} {
            color: #{{colour}};
        }
        {{/ticket.archivedRoles}}

        /* Scrollbar styling */
        ::-webkit-scrollbar {
            width: 8px;
        }

        ::-webkit-scrollbar-track {
            background: #2b2d31;
        }

        ::-webkit-scrollbar-thumb {
            background: #1a1b1e;
            border-radius: 4px;
        }

        ::-webkit-scrollbar-thumb:hover {
            background: #232428;
        }

        /* Reply styling */
        .reply-context {
            display: flex;
            align-items: center;
            gap: 4px;
            color: #80848e;
            font-size: 14px;
            margin-bottom: 4px;
            padding-left: 56px;
        }

        .reply-context::before {
            content: '';
            width: 32px;
            height: 12px;
            border-left: 2px solid #4e5058;
            border-top: 2px solid #4e5058;
            border-top-left-radius: 6px;
            margin-right: 4px;
            margin-left: -32px;
        }

        .reply-author {
            font-weight: 500;
            color: #dbdee1;
        }

        .reply-content {
            color: #80848e;
            overflow: hidden;
            text-overflow: ellipsis;
            white-space: nowrap;
            max-width: 300px;
        }

        /* Responsive */
        @media (max-width: 768px) {
            .ticket-info-grid {
                grid-template-columns: 1fr;
            }
            
            .avatar {
                width: 32px;
                height: 32px;
                margin-right: 12px;
            }
            
            .embed {
                max-width: 100%;
            }
            
            .attachment {
                max-width: 100%;
            }
        }
    </style>
</head>
<body>
    <div class="transcript-container">
        <!-- Header -->
        <div class="header">
            <div class="header-content">
                <div class="channel-name">{{ channelName }}</div>
            </div>
        </div>

        <!-- Ticket Info -->
        <div class="ticket-info">
            <div class="ticket-info-content">
                <div class="ticket-info-grid">
                    <div class="info-item">
                        <span class="info-label">Ticket ID</span>
                        <span class="info-value">{{ ticket.id }}</span>
                    </div>
                    <div class="info-item">
                        <span class="info-label">Guild</span>
                        <span class="info-value">{{ guildName }}</span>
                    </div>
                    <div class="info-item">
                        <span class="info-label">Category</span>
                        <span class="info-value">{{ ticket.category.name }} #{{ ticket.number }}</span>
                    </div>
                    <div class="info-item">
                        <span class="info-label">Topic</span>
                        <span class="info-value">{{#ticket.topic}}{{ . }}{{/ticket.topic}}{{^ticket.topic}}<em>No topic</em>{{/ticket.topic}}</span>
                    </div>
                    <div class="info-item">
                        <span class="info-label">Created</span>
                        <span class="info-value">{{#ticket}}{{ createdAtFull }}{{/ticket}}</span>
                    </div>
                    <div class="info-item">
                        <span class="info-label">Created By</span>
                        <span class="info-value">{{#ticket.createdBy}}{{ displayName }} (@{{ username }}){{/ticket.createdBy}}</span>
                    </div>
                    <div class="info-item">
                        <span class="info-label">Closed</span>
                        <span class="info-value">{{#ticket}}{{ closedAtFull }}{{/ticket}}</span>
                    </div>
                    <div class="info-item">
                        <span class="info-label">Closed By</span>
                        <span class="info-value">{{#ticket.closedBy}}{{ displayName }} (@{{ username }}){{/ticket.closedBy}}{{^ticket.closedBy}}<em>Automated</em>{{/ticket.closedBy}}</span>
                    </div>
                    <div class="info-item">
                        <span class="info-label">Close Reason</span>
                        <span class="info-value">{{#ticket.closedReason}}{{ ticket.closedReason }}{{/ticket.closedReason}}{{^ticket.closedReason}}<em>No reason</em>{{/ticket.closedReason}}</span>
                    </div>
                    <div class="info-item">
                        <span class="info-label">Claimed By</span>
                        <span class="info-value">{{#ticket.claimedBy}}{{ displayName }} (@{{ username }}){{/ticket.claimedBy}}{{^ticket.claimedBy}}<em>Not claimed</em>{{/ticket.claimedBy}}</span>
                    </div>
                </div>
            </div>
        </div>

        <!-- Questions Section -->
        {{#ticket.questionAnswers.length}}
        <div class="questions-section">
            <div class="questions-title">
                <svg width="20" height="20" viewBox="0 0 24 24" fill="currentColor">
                    <path d="M12 2C6.48 2 2 6.48 2 12s4.48 10 10 10 10-4.48 10-10S17.52 2 12 2zm1 17h-2v-2h2v2zm2.07-7.75l-.9.92C13.45 12.9 13 13.5 13 15h-2v-.5c0-1.1.45-2.1 1.17-2.83l1.24-1.26c.37-.36.59-.86.59-1.41 0-1.1-.9-2-2-2s-2 .9-2 2H8c0-2.21 1.79-4 4-4s4 1.79 4 4c0 .88-.36 1.68-.93 2.25z"/>
                </svg>
                Questions
            </div>
            {{#ticket.questionAnswers}}
            <div class="question-item">
                <div class="question-label">{{ question.label }}</div>
                <div class="question-answer">{{#value}}{{ . }}{{/value}}{{^value}}<em>No answer</em>{{/value}}</div>
            </div>
            {{/ticket.questionAnswers}}
        </div>
        {{/ticket.questionAnswers.length}}

        <!-- Feedback Section -->
        {{#ticket.feedback}}
        <div class="feedback-section">
            <div class="feedback-title">Feedback</div>
            <div class="rating-stars">
                {{#ratingStars}}★{{/ratingStars}}{{#ratingEmpty}}☆{{/ratingEmpty}}
            </div>
            {{#comment}}
            <div class="feedback-comment">"{{ . }}"</div>
            {{/comment}}
            {{^comment}}
            <div class="feedback-comment"><em>No comment</em></div>
            {{/comment}}
        </div>
        {{/ticket.feedback}}

        <!-- Messages -->
        <div class="messages-container">
            <div class="divider">MESSAGES</div>
            
            {{#ticket.archivedMessages}}
            <div class="message-group">
                <div class="avatar">
                    {{#author.avatar}}
                    <img src="https://cdn.discordapp.com/avatars/{{ author.userId }}/{{ author.avatar }}.png?size=128" alt="{{ author.displayName }}" onerror="this.style.display='none'; this.parentElement.textContent='{{ author.displayName.0 }}'">
                    {{/author.avatar}}
                    {{^author.avatar}}
                    {{ author.displayName.0 }}
                    {{/author.avatar}}
                </div>
                <div class="message-content-wrapper">
                    <div class="message-header">
                        <span class="author-name role-{{ author.roleId }}">{{ author.displayName }}</span>
                        {{#author.bot}}<span class="bot-tag">BOT</span>{{/author.bot}}
                        <span class="timestamp">{{ createdAtTimestamp }}</span>
                        <span class="message-number">{{ number }}</span>
                        {{#isPinned}}<span class="pinned-badge">📌 PINNED</span>{{/isPinned}}
                    </div>
                    {{#content.content}}
                    <div class="message-text">{{ . }}</div>
                    {{/content.content}}
                    
                    <!-- Attachments -->
                    {{#content.attachments.length}}
                    <div class="attachments">
                        {{#content.attachments}}
                        {{#contentType}}
                        {{#isImage}}
                        <a href="{{ url }}" target="_blank">
                            <img src="{{ url }}" alt="{{ name }}" class="image-attachment" onerror="this.outerHTML='<div class=\'attachment\'><div class=\'attachment-info\'><a href=\'{{ url }}\' class=\'attachment-name\' target=\'_blank\'>{{ name }}</a></div></div>'">
                        </a>
                        {{/isImage}}
                        {{^isImage}}
                        <div class="attachment">
                            <svg class="attachment-icon" viewBox="0 0 24 24" fill="currentColor">
                                <path d="M14 2H6a2 2 0 0 0-2 2v16a2 2 0 0 0 2 2h12a2 2 0 0 0 2-2V8l-6-6zm-1 2l5 5h-5V4zM6 20V4h6v6h6v10H6z"/>
                            </svg>
                            <div class="attachment-info">
                                <a href="{{ url }}" class="attachment-name" target="_blank">{{ name }}</a>
                                {{#size}}<span class="attachment-size">{{ size }} bytes</span>{{/size}}
                            </div>
                        </div>
                        {{/isImage}}
                        {{/contentType}}
                        {{^contentType}}
                        <div class="attachment">
                            <svg class="attachment-icon" viewBox="0 0 24 24" fill="currentColor">
                                <path d="M14 2H6a2 2 0 0 0-2 2v16a2 2 0 0 0 2 2h12a2 2 0 0 0 2-2V8l-6-6zm-1 2l5 5h-5V4zM6 20V4h6v6h6v10H6z"/>
                            </svg>
                            <div class="attachment-info">
                                <a href="{{ url }}" class="attachment-name" target="_blank">{{ name }}</a>
                            </div>
                        </div>
                        {{/contentType}}
                        {{/content.attachments}}
                    </div>
                    {{/content.attachments.length}}
                    
                    <!-- Embeds -->
                    {{#content.embeds}}
                    <div class="embed" {{#color}}style="border-left-color: #{{ color.toString(16).padStart(6, '0') }}"{{/color}}>
                        <div class="embed-content">
                            {{#thumbnail.url}}
                            <img src="{{ thumbnail.url }}" alt="" class="embed-thumbnail">
                            {{/thumbnail.url}}
                            
                            {{#author}}
                            <div class="embed-author">
                                {{#iconURL}}<img src="{{ iconURL }}" alt="" class="embed-author-icon">{{/iconURL}}
                                <span class="embed-author-name">{{#url}}<a href="{{ url }}" target="_blank" style="color: inherit; text-decoration: none;">{{ name }}</a>{{/url}}{{^url}}{{ name }}{{/url}}</span>
                            </div>
                            {{/author}}
                            
                            {{#title}}
                            <div class="embed-title">{{#url}}<a href="{{ url }}" target="_blank" style="color: inherit;">{{ title }}</a>{{/url}}{{^url}}{{ title }}{{/url}}</div>
                            {{/title}}
                            
                            {{#description}}
                            <div class="embed-description">{{ . }}</div>
                            {{/description}}
                            
                            {{#fields.length}}
                            <div class="embed-fields">
                                {{#fields}}
                                <div class="embed-field {{#inline}}inline{{/inline}}">
                                    <div class="embed-field-name">{{ name }}</div>
                                    <div class="embed-field-value">{{ value }}</div>
                                </div>
                                {{/fields}}
                            </div>
                            {{/fields.length}}
                            
                            {{#image.url}}
                            <img src="{{ image.url }}" alt="" class="embed-image">
                            {{/image.url}}
                            
                            {{#footer}}
                            <div class="embed-footer">
                                {{#iconURL}}<img src="{{ iconURL }}" alt="" class="embed-footer-icon">{{/iconURL}}
                                <span class="embed-footer-text">{{ text }}{{#timestamp}} • {{ timestamp }}{{/timestamp}}</span>
                            </div>
                            {{/footer}}
                        </div>
                    </div>
                    {{/content.embeds}}
                </div>
            </div>
            {{/ticket.archivedMessages}}
        </div>

        <!-- Footer -->
        <div class="transcript-footer">
            <p>Transcript generated on {{ generatedAt }} • Discord Tickets</p>
            <p>{{ ticket.archivedMessages.length }} messages from {{ ticket.archivedUsers.length }} participants</p>
        </div>
    </div>
</body>
</html>
